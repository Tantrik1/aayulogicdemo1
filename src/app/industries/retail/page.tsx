import { RetailClient } from './RetailClient';

export const metadata = {
  title: 'Retail & Digital Commerce | Aayulogic Industries',
  description:
    'Multi-tenant headless storefronts, omnichannel inventory, order management, loyalty engines, and marketplace platforms for retailers and DTC brands.',
};

export default function RetailPage() {
  return <RetailClient />;
}
