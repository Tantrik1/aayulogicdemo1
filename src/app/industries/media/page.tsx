import { MediaClient } from './MediaClient';

export const metadata = {
  title: 'Media & Entertainment | Aayulogic Industries',
  description:
    'Automated print composition, web-to-print storefronts, real-time HLS/DASH streaming, DAM pipelines, and subscription paywalls for publishers and broadcasters.',
};

export default function MediaPage() {
  return <MediaClient />;
}
