import { BOTClient } from './BOTClient';

export const metadata = {
  title: 'Build-Operate-Transfer (BOT) | Aayulogic Talent',
  description:
    'Your offshore development centre — built, operated, and handed over. Three-phase model from entity formation to clean ownership transfer.',
};

export default function BOTPage() {
  return <BOTClient />;
}
