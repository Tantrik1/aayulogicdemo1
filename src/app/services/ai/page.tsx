import { AIServiceClient } from './AIServiceClient';

export const metadata = {
  title: 'AI & Automation | Aayulogic Services',
  description:
    'Production AI systems: RAG, agents, conversational AI, computer vision, and RPA. Built by engineers who ship — not researchers who publish.',
};

export default function AIServicePage() {
  return <AIServiceClient />;
}
